Meteor.startup( () => {
	/*let users = [
         	{name: "Admin", email: "admin@engagementletter.co", roles: [ 'admin' ]},
    
         ]
    
    _.each(users, ( user ) => {
    
     	let id;
    
      	id =   	Accounts.createUser({
		      	    email: user.email,
		      	    password: "password",
		      	    profile: { name: user.name }
        		})

      	Roles.addUsersToRoles(id, user.roles);

    });*/
})